# Architecture Version History

## v1.1 — current frozen version

Supersedes the initial v1.0 freeze before implementation began. The git history preserves the v1.0 documents. v1.1 closes governance and semantic gaps discovered during pre-implementation architecture review, including explicit Project identity, immutable revisions, typed command-plan contracts, deterministic reproducibility, stronger work-order contracts, reviewer independence, and protected frozen-spec verification.

## v1.0

Initial architectural freeze recorded in git history. It is historical and immutable; it is not the active implementation authority.
