# Frozen Baseline

The first commit establishes the architecture baseline only. Implementation must proceed through `docs/work-orders/WORK-001.md` onward.

## Authority

The authoritative files are the files under `spec/` plus the architecture governance files under `docs/`.

## Rule

Do not edit frozen specification documents during ordinary implementation work. Any required change creates an Architecture Change Request and a new architecture version before implementation continues.
